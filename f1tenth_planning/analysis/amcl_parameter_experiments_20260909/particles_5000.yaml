gpu_amcl_cpp:
  ros__parameters:
    # One-factor candidate: raise the fixed local cloud size.  Keep KLD
    # enabled and leave roughening unchanged so this isolates particle
    # sampling density as much as the existing adaptive sampler permits.
    num_particles: 5000
    min_particles: 2000
    max_particles: 5000
