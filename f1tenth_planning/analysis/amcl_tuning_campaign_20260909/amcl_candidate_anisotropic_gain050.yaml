gpu_amcl_cpp:
  ros__parameters:
    # A/B candidate: retain the accepted production geometry and quality
    # gates, but restore half of the along-track innovation to reduce the
    # measured high-speed odometry phase drift.
    local_scan_correction_xy_gain: 0.50
    local_scan_correction_cross_track_only: false
    local_scan_correction_along_track_gain: 0.50
