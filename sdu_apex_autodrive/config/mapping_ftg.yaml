ftg_node:
  ros__parameters:
    # Safe mapping speed. The normal FTG configuration remains the single
    # source for geometry and scan logic; only mapping speed is overridden.
    max_speed: 0.25
    min_speed: 0.08
    speed_full_range: 2.0
