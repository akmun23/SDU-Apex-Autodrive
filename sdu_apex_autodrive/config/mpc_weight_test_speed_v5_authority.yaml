/**:
  ros__parameters:
    # MPC-only authority candidate.  This keeps lateral error moderate and
    # heading deliberately small, while increasing speed tracking relative to
    # the production profile.  It is a test overlay, not production config.
    weight_e_y: 150.0
    weight_e_psi: 2.0
    weight_u: 250.0
    weight_u_overspeed: 250.0
    weight_target_speed_state: 20.0
    weight_v: 5.0
    weight_r: 1.5
    weight_steering_command: 3.0
    weight_steering_rate: 5.0
    weight_target_speed_rate: 0.5
    weight_steering_rate_change: 10.0
    weight_target_speed_rate_change: 5.0
    terminal_multiplier: 3.0
