five_lap_map_saver:
  ros__parameters:
    # Ground-truth lap count is permitted only in this development mapper;
    # the competition localization/controller stack does not subscribe to it.
    lap_count_topic: /autodrive/roboracer_1/lap_count
    save_map_service: /slam_toolbox/save_map
    completion_topic: /sdu/mapping_complete

    # The map name is written without an extension.  SLAM Toolbox creates the
    # matching .yaml and .pgm files in this directory.
    output_directory: /workspace/src/live_runs/maps
    map_name: track_map
    target_laps: 5

    settle_before_save_s: 8.0
    stop_before_save: true
    save_each_lap: true
    lap_snapshot_settle_s: 1.0
    # Preserve map-to-simulator-world alignment for offline map validation.
    provenance_enabled: true
    # Empty means <output_directory>/<map_name>.provenance.yaml, so changing
    # map_name cannot accidentally reuse another map's transform.
    provenance_file: ""
    provenance_map_frame: map
    provenance_world_frame: world
