# Phase 0 reproduction — 2026-09-16

Repository HEAD matches the pinned `44421c3a8678658df98b73f015b8ba35b0d73d3c`. The worktree is dirty; the source and artifact status is preserved in `baseline_44421c3.json`. The targeted test suite passes 67/67.

The fixed Unity-controller candidate was rescored on its seven recorded validation runs at origin stride 1. CORE/GUARD p95 values for `e_cross`, heading, `u`, `v`, `r`, and Euclidean position matched the saved `validation_profile` exactly at 0.10, 0.25, 0.50, and 0.75 seconds. At N30 CORE, the fitted profile is `0.04135 m` cross-track, `0.04020 rad` heading, `0.13666 m/s` `u`, `0.01030 m/s` `v`, and `0.09187 rad/s` yaw rate. The handoff's §1.2 table instead reports the artifact's `validation_canonical_scalar_baseline` row; it is not the fitted profile.

The current full-origin native static-wheel replay agrees with the saved `direct_unity_wheel_collider_benchmark_075_exact_unity_curve_20260916.json` at all six horizons and five primary states (11,145 origins; maximum absolute p95 delta `0.0`). The older `corrected_source` report was timestamped 17:58 +0200, before pinned commit `44421c3` at 18:38 +0200. That commit introduced the explicit Unity-wheel plant and its scorer. The older artifact has no implementation or transition-table hashes and is therefore retained as a historical pre-commit result, not compared as a current-model rerun, and not used to claim an improvement. Current input and implementation hashes are recorded in `native_wheel_replay_provenance.json`.

The suspension output remains artifact-only. The existing assembled transition rows do not include synchronized exact six-coordinate suspension state at each candidate origin; the prior zero-initialized experiment is not an exact-suspension oracle ablation. The v5 cross-schedule force screen does reproduce its pinned v4 summary and still does not justify a universal axle-force gain.

No production MPC or Unity vehicle physics was changed. The Unity checkout is on `bb0c5018b48b888faea405a0b0342729b5841dc9` and already contains extensive user changes. For capture, only `ModelIdentificationDiagnostics.cs` was adjusted: a bounded single-producer/single-consumer trace queue moves CSV formatting and disk writes off the physics thread; collision-stay rows remain downsampled to 10 Hz with exact enter/exit records retained. The disposable competition-scene player compiled successfully with `-batchmode` and without `-no-graphics`; it lives under `/tmp`. This only establishes build success—the asynchronous writer still needs a live cadence pilot before data capture.

The first live PP pilot proved the fixed-step trace can be joined to the causal 40 Hz bridge stream using the consumed command sequence. The recorded prefix had 1,116 contiguous requests, median source spacing 25 ms, and two intervals above 30 ms. After the recorder stopped, the still-running bridge later observed a 10.004 ms source interval at physics steps 62,415→62,425 and correctly latched neutral. That run is rejected for model fitting. The next capture must stop ROS and Unity together, and the buffered logger needs a bounded repeat to determine whether it reduces timing pressure.

The separate no-diagnostic legal-input N30 screen produced CORE p95 `e_cross=0.08307 m` and `u=0.12912 m/s` at 0.75 s; truth-initialized `u` p95 was `0.13061 m/s`. The user's `0.10 m/s` longitudinal threshold is therefore not met, and the run's maximum speed was only `8.4077 m/s`. No longitudinal coefficient was refit: changing initial state to simulator truth did not reduce this error, and earlier causal odometry/IMU observer attempts did not deliver an accepted improvement. The synchronized hidden-state capture remains the only untried Phase 1 action that can identify a concrete wheel/contact transition mechanism.
