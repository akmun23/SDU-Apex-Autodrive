diff --git a/Assets/Scripts/LIDAR.cs b/Assets/Scripts/LIDAR.cs
index f5b2c9e..cf39842 100644
--- a/Assets/Scripts/LIDAR.cs
+++ b/Assets/Scripts/LIDAR.cs
@@ -104,11 +104,28 @@ public class LIDAR : MonoBehaviour
 			laserRayDirections[i] = new Vector3(Mathf.Sin(angle), 0, Mathf.Cos(angle)) * MaximumLinearRange;
 		}
 
-		// Perform raycasts
-		RaycastHit[] hits = new RaycastHit[MeasurementsPerScan];
+		// Schedule the independent sensor rays on Unity's physics workers.  The
+		// original loop executes all 1080 Physics.Raycast calls on the main
+		// thread, which can consume the complete 25 ms telemetry budget on the
+		// compete scene.  This keeps the same origin, range, layer mask, and
+		// single-hit behavior while allowing the queries to run in parallel.
+		NativeArray<RaycastCommand> raycastCommands =
+			new NativeArray<RaycastCommand>(MeasurementsPerScan, Allocator.TempJob);
+		NativeArray<RaycastHit> hits =
+			new NativeArray<RaycastHit>(MeasurementsPerScan, Allocator.TempJob);
 		for (int i = 0; i < MeasurementsPerScan; i++)
 		{
-			if (Physics.Raycast(Head.transform.position, laserRayDirections[i], out hits[i], MaximumLinearRange, layer_mask))
+			raycastCommands[i] = new RaycastCommand(
+				Head.transform.position, laserRayDirections[i],
+				MaximumLinearRange, layer_mask, 1);
+		}
+		JobHandle raycastJob = RaycastCommand.ScheduleBatch(
+			raycastCommands, hits, 32, default(JobHandle));
+		raycastJob.Complete();
+
+		for (int i = 0; i < MeasurementsPerScan; i++)
+		{
+			if (hits[i].collider != null)
 			{
 				float hitDistance = hits[i].distance;
 				RangeArray[i] = (hitDistance > MinimumLinearRange) ? hitDistance.ToString() : "inf"; // Store range result
@@ -157,6 +174,9 @@ public class LIDAR : MonoBehaviour
 			Graphics.DrawMesh(LaserScanMesh, Vector3.zero, Quaternion.identity, LaserScanMaterial, visualizationLayerID);
 		}
 
+		raycastCommands.Dispose();
+		hits.Dispose();
+
 		// LOG LASER SCAN
 		/*
 		// Range Array
@@ -176,4 +196,4 @@ public class LIDAR : MonoBehaviour
 		Debug.Log(IntensityArrayString); // Log the `RangeArrayString` to Unity Console
 		*/
 	}
-}
\ No newline at end of file
+}
